{% layout "layout.liquid" %} {% block content %}
    <div class="container">
        {% if username != "" and username != null %}
        <div class="loggedin">
        Welcome, <b>{{ username }}</b> ! <a href="/users/logout"><button>Logout</button></a>
        </div>
        {% else %}
        <div>
            Welcome! Not a User?
            <a href="/users/signup"><button>Signup</button></a> | 
            <a href="/users/login"><button>Login</button></a>
            </div>
        {% endif %}
        <div></div>
        {% if username != "" and username != null %}
        {% endif %}
        <br><h2>My Collections:</h2><br>
        <div>{% for cards in userCollect %}
            <a href="/users/myCollections/{{ cards.id }}">{{ cards.name }}</a><br>
            {% endfor %}<br><br>
        </div>
        <div><form action="/users/myCollections" method="POST">
            <input type="text" name="name" placeholder="Name of New Collection">
            <input type="submit" value="Create A Collection">        
            </form>
        </div>
    </div>
{% endblock %}